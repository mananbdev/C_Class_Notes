#include <stdio.h>
int main()
{
  /*int a, b, c, d, e;
  int result;
  float modified_result;
  printf("Enter values of a ,b ,c ,d ,e ,f\n");
  scanf("%d %d %d %d %d", &a, &b, &c, &d, &e);
  result = a + b * c / d - e;
  modified_result = ((a + b) - e) * (c / (float)d);
  printf("Standard result is %d\n", result);
  printf("Modified result is %f\n", modified_result);*/

  /*int n, count = 0;
  printf("Enter the value of n\n");
  scanf("%d", &n);
  for (int i = 1; i <= n; i++)
  {
    if (n % i == 0)
    {
      count++;
    }
  }
  if (count == 2)
  {
    printf("Prime number\n");
  }
  else
  {
    printf("Not a prime number\n");
  }*/

  /*int arr[10],pos=0,neg=0;
  for(int i=0;i<10;i++){
    scanf("%d",&arr[i]);
  }
  for(int i=0;i<10;i++){
    if(arr[i]>0){
      pos++;
    }
    if(arr[i]<0){
      neg++;
    }
  }
  printf("No. of positive numbers in array are %d\n",pos);
  printf("No. of negative numbers in array are %d\n",neg);*/

    

  return 0;
}