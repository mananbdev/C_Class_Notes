#include <stdio.h>
int main()
{
    //int x,i, j, highestAge,l,number, temp;
    //printf("How many inputs you have to input");
    //x=scanf("%d",&number);
    //int age[number];
    //for (i=0;i<number;i++){
    //    scanf("%d",&age[i]);
    }
    // int ages[] = {20, 22, 18, 35, 48, 26, 87, 70};
    // int length = sizeof(ages) / sizeof(ages[0]);
    // highestAge = ages[0];
    // for (i = 0; i < length; i++)
    // {
    //     if (highestAge < ages[i])
    //     {
    //         highestAge = ages[i];
    //     }
    // }
    // printf("%d", highestAge);

    // for (i = 0; i < length; i++)
    //     for (j = i + 1; j < length; j++)
    //         if (ages[i] > ages[j])
    //         {
    //             temp = ages[i];
    //             ages[i] = ages[j];
    //             ages[j] = temp;
    //         }
    // printf("Ascending order is : ");
    // for (i = 0; i < length; i++)
    // {
    //     printf("%d ", ages[i]);
    // }
    // printf("\n");
    // for (i = 0; i < length; i++)
    //     for (j = i + 1; j < length; j++)
    //         if (ages[i] < ages[j])
    //         {
    //             temp = ages[i];
    //             ages[i] = ages[j];
    //             ages[j] = temp;
    //         }
    // printf("Descending order is : ");
    //for (i = 0; i < number; i++)
    //{
    //   printf("%d ", age[i]);
    //}

